import 'package:flutter/material.dart';

class MensagensScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tela de Mensagens (em breve!)'),
    );
  }
}